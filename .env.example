# Neon PostgreSQL (URL completa)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# JWT
JWT_SECRET="tu-secreto-super-seguro-cambia-esto"
JWT_EXPIRES_IN="7d"

# Frontend
VITE_API_URL="http://localhost:5173"

# Frontend fixed admin account (single admin access to /admin)
VITE_ADMIN_EMAIL="admin@motorplace.com"
VITE_ADMIN_PASSWORD="Admin#2026"
